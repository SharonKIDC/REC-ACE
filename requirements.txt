absl-py==0.15.0  # Abseil Python is a library for writing Python code that better utilizes the features of the Abseil library.
appdirs==1.4.4  # Appdirs is a small Python module for determining appropriate platform-specific directories for your application to store data.
argon2-cffi==21.1.0  # Argon2-cffi is a Python CFFI binding for the Argon2 password hash algorithm.
astroid==2.8.4  # Astroid is a code analysis library used by pylint to look at Python source code and infer its type.
astunparse==1.6.3  # Astunparse is a Python library that provides an unparser for the Abstract Syntax Tree (AST) of Python code.
atomicwrites==1.4.0  # Atomicwrites provides atomic file writes.
attrs==21.2.0  # Attrs is a Python library for class attributes without boilerplate.
audioread==2.1.9  # Audioread is a Python library to work with audio files.
backcall==0.2.0  # Backcall is a Python library providing the `history-search-backward` functionality in the IPython shell.
bleach==4.1.0  # Bleach is an HTML sanitization library that escapes or strips markup and attributes based on a whitelist.
blis==0.7.5  # Blis is a fast, memory-efficient BLAS-like linear algebra library for Python.
branca==0.4.2  # Branca is a Python library for creating and displaying Leaflet-based interactive maps.
cachetools==4.2.4  # Cachetools is a set of tools for caching and memoization in Python.
catalogue==2.0.6  # Catalogue is a utility library for supporting runtime extension of Python classes with catalogued functions.
certifi==2021.10.8  # Certifi is a Python library containing a set of Mozilla's carefully curated collection of Root Certificates for validating SSL certificates.
cffi==1.15.0  # CFFI is a foreign function interface for Python calling C code.
chardet==5.1.0  # Chardet is a Python library for character encoding detection.
charset-normalizer==2.0.7  # Charset-normalizer is a Python library to detect the charset encoding and normalize it to UTF-8.
click==8.0.3  # Click is a Python package for creating beautiful command-line interfaces.
conllutils==1.1.4  # Conllutils is a Python library for working with CoNLL-formatted data.
cycler==0.10.0  # Cycler is a Python library for composable cycles.
cymem==2.0.6  # Cymem is a Python library for managing memory in Cython.
debugpy==1.5.1  # Debugpy is a Python debugger package providing a debugging server and debugging client.
decorator==5.1.0  # Decorator is a Python library for improving the syntax of Python function decorators.
defusedxml==0.7.1  # Defusedxml is a Python library for parsing XML and protecting against various XML vulnerabilities.
entrypoints==0.3  # Entrypoints is a Python library for discovering and loading entry points from installed packages.
ffmpeg-python==0.2.0  # Ffmpeg-python is a Python library for using ffmpeg to process media files.
filelock==3.3.1  # Filelock is a Python library for platform-independent file locking.
flatbuffers==2.0  # Flatbuffers is a Python library for serialization.
folium==0.12.1.post1  # Folium is a Python library for creating interactive leaflet maps.
fsspec==2023.6.0  # Fsspec is a Python library for managing filesystem-like objects.
future==0.18.3  # Future is a Python library for backwards-compatible Python 2/3 compatibility.
gast==0.4.0  # Gast is a Python library for parsing and transforming abstract syntax trees.
google-auth==2.3.2  # Google-auth is a Python library for Google Authentication.
google-auth-oauthlib==0.4.6  # Google-auth-oauthlib is a Python library for Google Authentication using OAuthlib.
google-pasta==0.2.0  # Google-pasta is a Python library for converting TensorFlow graphs to a flat representation.
graphviz==0.17  # Graphviz is a Python library for creating and rendering graph visualizations.
grpcio==1.41.1  # Grpcio is a Python library for implementing gRPC (Google Remote Procedure Calls) servers and clients.
h5py==3.6.0  # H5py is a Python library for working with HDF5 files (Hierarchical Data Format).
huggingface-hub==0.16.2  # Huggingface-hub is a Python library for publishing, discovering, and installing models and other files on the Hugging Face model hub.
idna==3.3  # Idna is a Python library for handling Internationalized Domain Names in Applications (IDNA).
importlib-metadata==5.1.0  # Importlib-metadata is a Python library for accessing metadata about Python packages.
iniconfig==1.1.1  # Iniconfig is a Python library for handling configuration files in the INI format.
ipykernel==6.4.2  # IPykernel is a Python kernel for Jupyter.
ipython==7.28.0  # IPython is a command-line interactive Python shell.
ipython-genutils==0.2.0  # IPython-genutils is a collection of utilities for IPython.
ipywidgets==7.6.5  # IPywidgets is a library for creating interactive widgets in the Jupyter notebook.
isort==5.9.3  # Isort is a Python library for sorting imports alphabetically and automatically separating them into sections.
jedi==0.18.0  # Jedi is a Python library for autocompletion, static analysis, and refactoring support in IDEs and text editors.
Jinja2==3.0.2  # Jinja2 is a Python library for creating and rendering templates.
joblib==1.1.0  # Joblib is a Python library for lightweight pipelining with Python functions as jobs.
jsonlines==2.0.0  # Jsonlines is a Python library for working with JSON lines format.
jsonpickle==3.0.1  # Jsonpickle is a Python library for serializing complex Python objects to JSON and back.
jsonschema==4.1.2  # Jsonschema is a Python library for validating JSON data against a given schema.
jupyter==1.0.0  # Jupyter is a package containing the core Jupyter application.
jupyter-client==7.0.6  # Jupyter-client is a Python library for interacting with Jupyter kernels.
jupyter-console==6.4.0  # Jupyter-console is a Python library for Jupyter console application.
jupyter-core==4.9.0  # Jupyter-core is a Python library for Jupyter core services.
jupyter-http-over-ws==0.0.8  # Jupyter-http-over-ws is a Python library for upgrading WebSocket connections to HTTP/1.1.
jupyterlab-pygments==0.1.2  # Jupyterlab-pygments is a Python library for Pygments lexer and formatter for JupyterLab.
jupyterlab-widgets==1.0.2  # Jupyterlab-widgets is a Python library for Jupyter interactive widgets.
jupytext==1.13.0  # Jupytext is a Python library for pairing Jupyter Notebooks with scripts or Markdown files.
keras==2.7.0  # Keras is a high-level neural networks API written in Python.
Keras-Preprocessing==1.1.2  # Keras-Preprocessing is a data preprocessing library for Keras.
kiwisolver==1.3.2  # Kiwisolver is a fast solver for linear programming problems.
langcodes==3.3.0  # Langcodes is a Python library for working with language tags.
lazy-object-proxy==1.6.0  # Lazy-object-proxy is a fast and simple proxy for Python objects.
libclang==12.0.0  # Libclang is a Python library for using the Clang compiler as a Python library.
llvmlite==0.37.0  # Llvmlite is a lightweight LLVM python binding for writing JIT compilers.
Markdown==3.3.4  # Markdown is a Python library for parsing and rendering Markdown.
markdown-it-py==1.1.0  # Markdown-it-py is a Python library for markdown parsing and tokenization.
MarkupSafe==2.0.1  # Markupsafe is a Python library for escaping any characters that might be used for HTML injection attacks.
matplotlib==3.4.3  # Matplotlib is a Python library for creating static, animated, and interactive visualizations in Python.
matplotlib-inline==0.1.3  # Matplotlib-inline is a backend for matplotlib to work with IPython inline backend.
mccabe==0.6.1  # Mccabe is a Python library for complexity checker using McCabe's cyclomatic complexity metric.
mdit-py-plugins==0.2.8  # Mdit-py-plugins is a collection of markdown-it-py plugins.
mistune==0.8.4  # Mistune is a Python library for markdown parsing, rendering, and simple HTML escaping.
more-itertools==9.0.0  # More-itertools is a Python library for providing more useful iterators than the built-in ones.
murmurhash==1.0.6  # Murmurhash is a Python library for generating a 32-bit hash value for a given input.
musdb==0.4.0  # Musdb is a Python library for working with the MUSDB18 dataset of music tracks.
museval==0.4.0  # Museval is a Python library for evaluating audio source separation performance.
music21==8.1.0  # Music21 is a Python library for musicology-related software.
mypy==0.910  # Mypy is a static type checker for Python.
mypy-extensions==0.4.3  # Mypy-extensions is a Python library for optional static typing extensions used by mypy.
nbclient==0.5.4  # Nbclient is a Python library for Jupyter notebook client APIs.
nbconvert==6.2.0  # Nbconvert is a Python library for converting Jupyter notebooks to various formats.
nbformat==5.1.3  # Nbformat is a Python library for reading and writing Jupyter notebook format.
nest-asyncio==1.5.1  # Nest-asyncio is a Python library for patching asyncio to allow nested event loops.
nltk==3.8.1  # Nltk is a Python library for natural language processing.
notebook==6.4.5  # Notebook is a web-based interactive computational environment for creating Jupyter notebooks.
numba==0.54.1  # Numba is a Python library for just-in-time (JIT) compiling functions for faster numerical computing.
numpy==1.20.3  # Numpy is a Python library for numerical computing with arrays.
oauthlib==3.1.1  # OAuthlib is a Python library for implementing OAuth providers and consumers.
opencv-python==4.5.4.58  # Opencv-python is a Python library for computer vision tasks.
opt-einsum==3.3.0  # Opt-einsum is a Python library for optimizing Einsum functions.
packaging==21.0  # Packaging is a Python library for core utilities for Python packages.
pandas==1.3.4  # Pandas is a Python library for data manipulation and analysis.
pandoc==2.3  # Pandoc is a Haskell library for converting between markup formats.
pandocfilters==1.5.0  # Pandocfilters is a Python library for writing pandoc filters in python.
parso==0.8.2  # Parso is a Python library for parsing Python source code.
pathy==0.6.1  # Pathy is a Python library for working with filesystem paths.
pickleshare==0.7.5  # Pickleshare is a Python library for persistent dictionaries using Python pickles.
Pillow==8.4.0  # Pillow is a Python Imaging Library for working with images.
platformdirs==2.4.0  # Platformdirs is a Python library for getting directories for storing user data, config, cache, etc. on different platforms.
pluggy==1.0.0  # Pluggy is a Python library for writing plugin-based systems.
plumbum==1.8.0  # Plumbum is a Python library for easily writing shell scripts.
ply==3.11  # Ply is a Python library for parsing and generating LR(1) parse tables.
pooch==1.5.2  # Pooch is a Python library for managing and caching remote data.
preshed==3.0.6  # Preshed is a Cython hash table for precomputed hashes.
prometheus-client==0.11.0  # Prometheus-client is a Python library for creating Prometheus metrics.
prompt-toolkit==3.0.21  # Prompt-toolkit is a library for building interactive command-line applications.
protobuf==3.19.0  # Protobuf is a Python library for Protocol Buffers, Google's data interchange format.
py==1.10.0  # Py is a Python library for querying Python installations.
pyaml==21.10.1  # Pyaml is a Python library for serializing and deserializing YAML.
pyasn1==0.4.8  # Pyasn1 is a Python library for implementing ASN.1 types and DER/BER/CER codecs.
pyasn1-modules==0.2.8  # Pyasn1-modules is a Python library for implementing ASN.1 modules.
pycparser==2.20  # Pycparser is a complete parser of the C language, written in pure Python.
pydantic==1.8.2  # Pydantic is a Python library for data validation and settings management.
pyee==8.2.2  # Pyee is a Python library for implementing EventEmitter pattern.
Pygments==2.10.0  # Pygments is a syntax highlighting package written in Python.
pylint==2.11.1  # Pylint is a Python library for code analysis, code check, and code style enforcement.
pyparsing==3.0.3  # Pyparsing is a Python library for parsing text files.
pyppeteer==0.2.6  # Pyppeteer is a Python library for using headless Chrome.
pyrsistent==0.18.0  # Pyrsistent is a Python library for persistent data structures.
pytest==6.2.5  # Pytest is a testing framework for Python.
python-dateutil==2.8.2  # Python-dateutil is a Python library for manipulating dates and times.
pytz==2021.3  # Pytz is a Python library for working with timezones.
pywin32==302  # Pywin32 is a set of Python extensions for Microsoft Windows.
pywinpty==1.1.4  # Pywinpty is a Python library for enabling Unix terminal functionalities on Windows.
PyYAML==6.0  # PyYAML is a YAML parser and emitter for Python.
pyzmq==22.3.0  # Pyzmq is a Python library for ZeroMQ, an asynchronous messaging library.
qtconsole==5.1.1  # Qtconsole is a Jupyter Qt console widget.
QtPy==1.11.2  # QtPy is a lightweight abstraction layer for the PyQt and PySide libraries.
regex==2023.3.23  # Regex is a Python library for working with regular expressions.
requests==2.26.0  # Requests is a Python library for making HTTP requests.
requests-oauthlib==1.3.0  # Requests-oauthlib is a Python library for OAuth support for requests.
resampy==0.2.2  # Resampy is a Python library for audio resampling.
rsa==4.7.2  # Rsa is a Python library for generating and verifying RSA public and private keys.
safetensors==0.3.1  # Safetensors is a Python library for static analysis of TensorFlow models.
scikit-learn==1.0.1  # Scikit-learn is a Python library for machine learning.
scipy==1.7.1  # Scipy is a Python library for scientific computing and technical computing.
seaborn==0.11.2  # Seaborn is a Python library for statistical data visualization based on matplotlib.
Send2Trash==1.8.0  # Send2Trash is a Python library for sending files to the Trash or Recycle Bin.
simplejson==3.18.4  # Simplejson is a Python library for fast JSON encoding and decoding.
six==1.16.0  # Six is a Python 2 and 3 compatibility library.
smart-open==5.2.1  # Smart-open is a Python library for efficient streaming of very large files from/to cloud storage.
SoundFile==0.10.3.post1  # SoundFile is a Python library for reading and writing audio files.
spacy-legacy==3.0.8  # Spacy-legacy is a Python library for legacy utilities and functionalities for spaCy.
spacy-loggers==1.0.1  # Spacy-loggers is a Python library for spaCy's logging.
srsly==2.4.2  # Srsly is a Python library for serialization and deserialization of various data types.
stempeg==0.2.3  # Stempeg is a Python library for reading/writing musical audio data in STEM format.
tabulate==0.9.0  # Tabulate is a Python library for printing tabular data in a visually appealing ASCII table format.
tensorboard==2.7.0  # Tensorboard is a suite of web applications for inspecting and understanding your TensorFlow runs.
tensorboard-data-server==0.6.1  # Tensorboard-data-server is a Python library for serving tensorboard data.
tensorboard-plugin-wit==1.8.0  # Tensorboard-plugin-wit is a What-If Tool (WIT) plugin for TensorBoard.
termcolor==1.1.0  # Termcolor is a Python library for ANSI color formatting in terminal text.
terminado==0.12.1  # Terminado is a Python library for allowing web-based terminals.
testpath==0.5.0  # Testpath is a Python library for test utilities for code working with files and commands.
thinc==8.0.13  # Thinc is a Python library for efficient neural network primitives.
threadpoolctl==3.0.0  # Threadpoolctl is a Python library for efficient management of thread pools.
tokenizers==0.13.3  # Tokenizers is a Python library for fast tokenization of natural language text.
toml==0.10.2  # Toml is a Python library for parsing and creating TOML (Tom's Obvious, Minimal Language).
torch==1.12.1  # Torch is a Python library for machine learning using PyTorch.
torchsummary==1.5.1  # Torchsummary is a PyTorch summary writer.
torchviz==0.0.2  # Torchviz is a PyTorch visualization library.
tornado==6.1  # Tornado is a Python library for scalable, non-blocking web server and web application framework.
tqdm==4.62.3  # Tqdm is a Python library for showing progress bar and ETA estimates.
traitlets==5.1.1  # Traitlets is a Python library for specifying attributes with type checking, validation, and default values.
transformers==4.30.2  # Transformers is a Python library for natural language processing using transformer models.
typer==0.4.0  # Typer is a Python library for building command-line applications.
typing-extensions==3.10.0.2  # Typing-extensions is a Python library for backported and experimental typing features.
urllib3==1.26.7  # Urllib3 is a Python library for making HTTP requests.
wasabi==0.9.0  # Wasabi is a Python library for formatting text with ANSI colors.
wcwidth==0.2.5  # Wcwidth is a Python library for measuring terminal string width.
webcolors==1.12  # Webcolors is a Python library for working with color names and color value formats.
webencodings==0.5.1  # Webencodings is a Python library for character encodings for the web.
websockets==9.1  # Websockets is a Python library for working with WebSockets.
Werkzeug==2.0.2  # Werkzeug is a Python library for WSGI utilities and web application framework.
widgetsnbextension==3.5.1  # Widgetsnbextension is a Jupyter notebook extension for adding widget management.
wrapt==1.13.2  # Wrapt is a Python library for function/method decorators, and lightweight wrappers.
xlrd==2.0.1  # Xlrd is a Python library for reading data and formatting information from Excel files.
zipfile36==0.1.3  # Zipfile36 is a backport of Python 3.6's zipfile module to earlier Python versions.
zipp==3.11.0  # Zipp is a Python library for efficient Zip archive I/O.
